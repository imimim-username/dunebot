# Discord Bot Configuration
# Required: Get your bot token from: https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your-discord-bot-token-here

# Optional: Restrict bot to a specific guild (server) for faster command syncing during development
# Get your server ID by enabling Developer Mode in Discord, then right-click your server
DISCORD_GUILD_ID=your-server-id-for-fast-sync

# Dune Analytics Configuration
# Required: Get your API key from: https://dune.com/settings/api
DUNE_API_KEY=your-dune-api-key-here

# Optional: Delay in seconds between sending embeds (default: 10)
# This helps avoid Discord rate limits when sending multiple embeds
EMBED_DELAY_SECONDS=10

# Scheduled Execution Configuration (Optional)
# These settings are only needed if you want the bot to automatically execute queries on a schedule

# The Dune query ID to execute automatically every 24 hours
# SCHEDULED_QUERY_ID=1234567

# Local time to execute the query (HH:MM format, 24-hour)
# Example: "14:30" for 2:30 PM, "09:00" for 9:00 AM
# SCHEDULED_EXECUTION_TIME=14:30

# Discord channel ID where scheduled query results should be posted
# Get this by right-clicking the channel (with Developer Mode enabled) and selecting "Copy Channel ID"
# DISCORD_CHANNEL_ID=999999999999999999
